FINAL PASS ONLY

This is the last allowed turn for the current run. Tools are disabled until the user replies. Respond with text only.

STRICT REQUIREMENTS:
1. Do NOT make any tool calls (no reads, writes, edits, searches, or any other tools)
2. Give a concise operator handoff:
   - what was confirmed
   - what is still missing
   - what should happen next
3. Do not dramatize the limit or repeat internal budget mechanics unless the user explicitly asked about them.

Any attempt to use tools is a critical violation. Respond with text only.
