[package]
name = "dax-ledger-bin"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "dax-ledger"
path = "src/main.rs"

[dependencies]
dax-ledger = { path = "../dax-ledger" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
